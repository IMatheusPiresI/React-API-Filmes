import React from "react";
import { ContainerHeader, TitleHeader } from "./HeaderStyle";

const Header = () =>{
    return(
        <ContainerHeader>
            <TitleHeader>Search The Movie</TitleHeader>
        </ContainerHeader>
    )
}

export default Header;